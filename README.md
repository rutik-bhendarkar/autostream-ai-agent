# AutoStream AI Agent

## Overview
This project is a conversational AI agent that converts user conversations into qualified leads.

## Features
- Intent Detection (Greeting, Inquiry, High Intent)
- RAG-based responses using local JSON knowledge base
- Lead capture system
- Stateful conversation memory

## How to Run

```bash
pip install -r requirements.txt
python main.py